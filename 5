HELP = """
help - напечатать справку по программе.
add - добавить задачу в список.
show - напечатаь все добавленные задачи.
exit - завершить программу"""
tomorrow = []
today = []
other = []
tasks = []
run = True
while run:
  command = input("Введите команду: ")
  if command == "help":
    print(HELP)
  elif command == "show":
    print(tasks)
  elif command == "add":
    task = input("Введите название задачи: ")
    tasks.append(task)
    while run:
      data = input("Введите дату задачи:   ")
      if data == "today":
        tasks.append(today)
      elif data == "tomorrow":
        tasks.append(tomorrow)
      else: tasks.append(other)
      break
    print("Задача добавленна")
  elif command == "exit":
    print("Спасибо за использование! До свидания!")
    break
  else:
    print("неизвестная команда")
    
